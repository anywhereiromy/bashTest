
    const {expect} = require("chai")
    const { marta, romy} = require("../index.js")
    
    describe("", () => {
        it("", () => {
            expect(romy()).to.equal(marta());
        })
    })
